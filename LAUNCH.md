# Launch — SignalX

Double-click in Finder:

`~/Developer/projects/Launchers/SignalX.command`

Or from this folder:

```bash
cd /Users/cameroncohen/Developer/projects/SignalX
./run-dev.sh
```

| | |
|---|---|
| Launcher | `SignalX.command` |
| Kind | runnable |
| Managed by | `~/Developer/projects/Launchers/scripts/sync-projects` |

Re-run sync after changing how this project starts:

```bash
~/Developer/projects/Launchers/scripts/sync-projects
```
